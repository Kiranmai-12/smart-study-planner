let tasks=[];function addTask(){const subject=document.getElementById("subject").value.trim();const hours=document.getElementById("hours").value;if(!subject||!hours){alert("Please enter a subject and study hours.");return}tasks.push({subject,hours,completed:false});document.getElementById("subject").value="";document.getElementById("hours").value="";displayTasks()}function toggleTask(i){tasks[i].completed=!tasks[i].completed;displayTasks()}function deleteTask(i){tasks.splice(i,1);displayTasks()}function displayTasks(){const list=document.getElementById("taskList");const empty=document.getElementById("empty");list.innerHTML="";empty.style.display=tasks.length?"none":"block";tasks.forEach((task,i)=>{const div=document.createElement("div");div.className="task"+(task.completed?" completed":"");div.innerHTML=`<span><strong>${task.subject}</strong> — ${task.hours} hour(s)</span><span><button onclick="toggleTask(${i})">${task.completed?"Undo":"Done"}</button> <button onclick="deleteTask(${i})">Delete</button></span>`;list.appendChild(div)});updateProgress()}function updateProgress(){const completed=tasks.filter(t=>t.completed).length;const percent=tasks.length?Math.round(completed/tasks.length*100):0;document.getElementById("bar").style.width=percent+"%";document.getElementById("progressText").textContent=percent+"% completed"}